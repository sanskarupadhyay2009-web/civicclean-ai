// CityPlatform is now rendered inside CityScene's SVG.
// This stub exists so import in CityScene (if referenced elsewhere) won't break.
function CityPlatform() { return null; }
export default CityPlatform;
